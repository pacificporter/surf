name: golangci-lint
on:
  workflow_dispatch:
  push:
jobs:
  golangci:
    name: lint
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v3.5.1
      - name: golangci-lint
        uses: golangci/golangci-lint-action@v3
        with:
          version: v1.40.0
